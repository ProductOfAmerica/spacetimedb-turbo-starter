{
	"name": "server",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"spacetime": "docker run -d --rm --pull always -p 3000:3000 clockworklabs/spacetime start",
		"build": "spacetime build",
		"publish:local": "spacetime publish --server local server",
		"publish:cloud": "spacetime login && spacetime publish server",
		"logs": "spacetime logs server"
	}
}
